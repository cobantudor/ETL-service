def getInsert():
    return """
        INSERT INTO user_orders(
                id,
                created_at,
                date_tz,
                item_count,
                order_id,
                receive_method,
                status,
                store_id,
                subtotal,
                tax_percentage,
                total,
                total_discount,
                total_gratuity,
                total_tax,
                updated_at,
                user_id,
                fulfillment_date_tz,
                user_first_name,
                user_last_name,
                user_merchant_id,
                user_phone_number,
                user_created_at,
                user_updated_at)
        values (
                %(id)s,
                %(created_at)s,
                %(date_tz)s,
                %(item_count)s,
                %(order_id)s,
                %(receive_method)s,
                %(status)s,
                %(store_id)s,
                %(subtotal)s,
                %(tax_percentage)s,
                %(total)s,
                %(total_discount)s,
                %(total_gratuity)s,
                %(total_tax)s,
                %(updated_at)s,
                %(user_id)s,
                %(fulfillment_date_tz)s,
                %(user_first_name)s,
                %(user_last_name)s,
                %(user_merchant_id)s,
                %(user_phone_number)s,
                %(user_created_at)s,
                %(user_updated_at)s)
        ON CONFLICT (id) DO UPDATE SET 
            created_at=%(created_at)s,
            date_tz=%(date_tz)s,
            item_count=%(item_count)s,
            order_id=%(order_id)s,
            receive_method=%(receive_method)s,
            status=%(status)s,
            store_id=%(store_id)s,
            subtotal=%(subtotal)s,
            tax_percentage=%(tax_percentage)s,
            total=%(total)s,
            total_discount=%(total_discount)s,
            total_gratuity=%(total_gratuity)s,
            total_tax=%(total_tax)s,
            updated_at=%(updated_at)s,
            user_id=%(user_id)s,
            fulfillment_date_tz=%(fulfillment_date_tz)s,
            user_first_name=%(user_first_name)s,
            user_last_name=%(user_last_name)s,
            user_merchant_id=%(user_merchant_id)s,
            user_phone_number=%(user_phone_number)s,
            user_created_at=%(user_created_at)s,
            user_updated_at=%(user_updated_at)s
    """